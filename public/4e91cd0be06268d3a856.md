---
title: HLSLの関数をShaderGraph用に自動変換する
tags:
  - photoshop
  - Unity
  - HLSL
  - ShaderGraph
private: false
updated_at: '2021-09-28T01:21:09+09:00'
id: 4e91cd0be06268d3a856
organization_url_name: null
slide: false
ignorePublish: false
---


## 問題点

PhotoShopMath.hlsl や Quaternion.hlsl のようなHLSLのよく使う[汎用関数](https://github.com/supertask/ShaderLibLegacy/blob/main/Packages/jp.supertask.shaderliblegacy/Shader/Lib/PhotoShopMath.hlsl) をShaderGraphで使いたい場合、

```
float3 BlendDarken(float3 baseColor, float3 blendColor)
{
    return min(blendColor, baseColor);
}
```

 の形式の関数を

```
void BlendDarkenShaderGraph_float(float3 baseColor, float3 blendColor, out float3 res)
{
	res = BlendDarken(baseColor, blendColor);
}
```

の関数に毎回変換する（ラップする）のが、すごくめんどくさい問題があります。実際、PhotoShopMath.hlsl などは30関数以上あるので、めっちゃめんどいです。

## 解決法

PhotoShopMath.hlsl や Quaternion.hlslなどの関数をラップする関数をPythonで自動生成して、その関数をShader Graphで使えるようにする。実際の自動生成するPythonプログラムは下の通りです。

```python
#
# Shader functions to ShaderGraph converter
#
#
# How to use
#   1. Create the file "./functions.hlsl"
#   2. Execute `python shaderGraphFunctionGen.py`
#   3. Write the input file path "#inlude ./<functions>.hlsl" on "./output_functions.hlsl"
#
# INPUT (functions.hlsl):
#
# float3 RGBToHSL(float3 color)
#
#
# OUTPUT (output_functions.hlsl):
# 
# void RGBToHSLShaderGraph_float(float3 color, out float3 res)
# {
#	res = RGBToHSL(color);
# }
#

import os
import re
import sys

input_file_path = "./functions.hlsl"
output_file_path = "./output_functions.hlsl"

if not os.path.isfile(input_file_path):
	print("Error: create the file, '%s'." % input_file_path)
	sys.exit(1)

r_func_name = re.compile(r"(\w+) (\w+)\((.*)\)")
r_func_params = re.compile(r"(\w+) (\w+)")

rf = open(input_file_path, "r")
wf = open(output_file_path, "w")

for line in rf:
	line = line.rstrip()

	m = r_func_name.search(line)
	#print(m.group(0))
	func_res_type = m.group(1)
	func_name = m.group(2)
	func_params_type = m.group(3)
	# print("func res, func name: %s, %s" % (func_res_type, func_name))


	res = r_func_params.finditer(func_params_type)
	variables = []
	for m in res:
		# param_type = m.group(1)
		param_name = m.group(2)
		# print("%s, %s" % (param_type, param_name))
		variables.append(param_name)
	func_params = ', '.join(variables)


	output_str = f"""void {func_name}ShaderGraph_float({func_params_type}, out {func_res_type} res)
{{
	res = {func_name}({func_params});
}}"""
	#print(output_str)

	wf.write(output_str + "\n\n")

rf.close()
wf.close()
```

一応、Gistにも載せています。

https://gist.github.com/supertask/ce64a8b0580b8bdebfa2c21c6db443cf


ちなみに、最終的に今回のPythonスクリプトで吐き出した `PhotoShopMath.hlsl` の ShaderGraph版は以下のリンクに載せています。

https://github.com/supertask/ShaderLibURP/blob/main/Packages/jp.supertask.shaderliburp/Shader/Lib/PhotoShopMathSG.hlsl


- [Twitterアカウント](https://twitter.com/supertask_jp)
- [YouTubeアカウント](https://www.youtube.com/c/TasukuTAKAHASHI)

